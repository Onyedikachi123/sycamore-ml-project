# Backend Documentation

For setup instructions, please refer to [SETUP.md](SETUP.md).

## Quick Start from Root

```bash
chmod +x setup.sh
./setup.sh
```

## Manual Setup

The virtual environment is explicitly excluded from the repository. Do not
commit `venv` or `.venv`.

See `SETUP.md` for detailed requirements (Python 3.11, OpenMP).
